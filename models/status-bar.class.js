/**
 * Base class for user interface status bar components (e.g., health, coin counter).
 * Serves as a foundational container for UI elements rendered on canvas.
 * Inherits from DrawableObject.
 * @extends DrawableObject
 */
class Statusbar extends DrawableObject {
  /**
   * Creates an instance of Statusbar.
   */
  constructor() {
    super();
  }
}